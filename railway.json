{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && node serve.mjs --version || true"
  },
  "deploy": {
    "startCommand": "node serve.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
